[project]
name = "silverflow"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "duckdb",
    "dbt-duckdb",
    "faker",
    "pandas",
    "jupyter",
    "datasette",
    "matplotlib>=3.10.8",
    "seaborn>=0.13.2",
    "scipy>=1.17.1",
]
